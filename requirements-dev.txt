# Development dependencies
pytest~=9.1.1
pytest-asyncio~=1.4.0
pytest-cov~=7.1.0
ruff~=0.16.2
# ty replaces pyright, and ruff's PL/D/ARG rules replace pylint.
#
# Pinned to an exact version, not a ~= range, because ty is pre-1.0 and its
# diagnostics still move between releases. A floating pin would let a routine
# dependency update turn a green PR red for reasons unrelated to the PR.
ty==0.0.78
