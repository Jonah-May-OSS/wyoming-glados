# Development dependencies
pytest~=9.1.1
pytest-asyncio~=1.4.0
pytest-cov~=7.1.0
ruff~=0.16.2
regex~=2026.7.19
pyright~=1.1.411
pylint~=4.0.6
# Stub-only package so pyright can resolve `import pandas` in the gladostts
# submodule without installing pandas itself (it pins torch/numpy versions that
# conflict with requirements.txt).
pandas-stubs~=3.0.5.260730